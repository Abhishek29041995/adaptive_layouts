enum AdaptiveNavigationMode {
  auto, // Decides automatically based on screen size
  bottom, // Forces BottomNavigationBar
  rail, // Forces NavigationRail
  sidebar, // Forces Sidebar
  both, // Uses both BottomNavigationBar & Sidebar
}
